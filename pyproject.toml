[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "morana"
version = "0.1.0"
description = "Morana: modern reactor analysis for hexagonal lattices."
authors = [{name = "Lubomir Bures"}]
keywords = [
  "neutronics",
  "reactor-physics",
  "nuclear-engineering",
  "neutron-diffusion",
  "multigroup-diffusion",
  "finite-volume-method",
  "hexagonal-lattice",
  "scientific-computing",
  "computational-physics",
]
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
  "numpy",
  "scipy",
  "matplotlib",
  "plotly",
  "pyyaml",
  "pydantic",
  "h5py",
]

[project.urls]
Documentation = "https://www.lubomirbures.com/morana/"
Source = "https://github.com/tannhorn/morana"
Issues = "https://github.com/tannhorn/morana/issues"
Changelog = "https://www.lubomirbures.com/morana/changelog.html"
Citation = "https://www.lubomirbures.com/morana/citation.html"
Release = "https://github.com/tannhorn/morana/releases/tag/v0.1.0"
DOI = "https://doi.org/10.5281/zenodo.22681316"

[project.optional-dependencies]
dev = [
  "black",
  "codespell>=2,<3",
  "mkdocs>=1.6,<2",
  "mkdocs-material>=9,<10",
  "mkdocstrings-python",
  "pylint",
  "pytest",
  "reuse",
  "vtk",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.pylint.main]
py-version = "3.12"

[tool.pylint.format]
max-line-length = 88

[tool.pylint.messages_control]
disable = [
  "too-many-arguments",
  "too-many-locals",
  "too-few-public-methods",
]

[tool.pylint.basic]
good-names = ["D", "i", "j", "k", "q", "r"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
